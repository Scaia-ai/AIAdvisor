# AIAdvisor Lambda documentation

* To deploy
  * ./package.sh <name-of-lambda, like fastapi>
  * You will get a zip on your Desktop
  * Upload that zip to AWS Lambda
  * I noticed that it only works on Ubuntu. Maybe move deployment to CloudShell